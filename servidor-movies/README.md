# servidor-movies

1. En una carpeta aparte, clona el repositorio:
   ```bash
   git clone 
   rm -rf .git
   git init; git add .; git commit -m "Initial commit"
   gh repo create
   si falla, usar. gh repo delete <nombre-repo> 
   rm -rf .git
```